import React, { Component } from 'react';
import { connect } from 'react-redux';
// import PropTypes from 'prop-types'

class EditExpenseForm extends Component {
  render() {
    return (
      <div />
    );
  }
}

export default connect(null, null)(EditExpenseForm);
