export const onePageInputChange = () => {
  const inputValue = document.getElementById('input-one-page');
  const resultNumber = inputValue.value;
  return resultNumber;
};

export default onePageInputChange;
